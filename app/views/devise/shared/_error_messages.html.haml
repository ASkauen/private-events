- if resource.errors.any?
  #error_explanation
    %ul
      - resource.errors.full_messages.each do |message|
        %li{class: "bg-yellow-200 text-center rounded-sm border border-yellow-500 my-2"}= message
