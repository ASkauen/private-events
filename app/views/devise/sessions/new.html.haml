.grid.grid-rows-auto.justify-items-center.mt-10.shadow-card.mx-auto.py-5.rounded-md{class:"w-1/3"}
  %h2 Sign in:
  = form_for(resource, as: resource_name, url: session_path(resource_name), html: {class: "mt-3"}) do |f|
    .field
      = f.label :email
      %br/
      = f.email_field :email, autofocus: true, autocomplete: "email", class: "px-2 border-2 rounded-md"
    .field
      = f.label :password
      %br/
      = f.password_field :password, autocomplete: "current-password", class: "px-2 border-2 rounded-md"
    - if devise_mapping.rememberable?
      .field
        = f.check_box :remember_me
        = f.label :remember_me
    .actions
      = f.submit "Log in", class: "cursor-pointer px-3 py-1 rounded-md mt-2 hover:bg-gray-200"
  .mt-10
    = render "devise/shared/links"
